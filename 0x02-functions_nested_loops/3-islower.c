#include "holberton.h"
/**
 *_islower - check the lowercase letters
 *@c: recibe a character
 *
 *Return: return 1 if lowercase, 0 if don't
 */
int _islower(int c)
{
	int a = 1, b = 0;

	if (c >= 97 && c <= 122)
	{
		return (a);
	}
	else
	{
		return (b);
	}
}
