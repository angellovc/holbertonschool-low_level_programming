#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
        char *n = "100";
        char *m = "1";
        char r[100];
        char r2[10];
        char r3[11];
        char *res;

	infinite_add(n, m, r);
	return (0);
}
